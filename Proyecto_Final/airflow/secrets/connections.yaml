# Connections saved here do not appear in the UI
# Postgres connection using the airflow database
pg_conn:
  conn_type: postgres
  host: postgres
  login: airflow
  password: airflow
  schema: airflow
